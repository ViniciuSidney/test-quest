# Layouts CSS

Esta pasta é reservada para layouts reutilizáveis do Test Quest.

O layout atual da aplicação permanece em `pages/test-quest.css` para preservar a ordem da cascata durante a migração inicial.
