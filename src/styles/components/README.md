# Componentes CSS

Esta pasta receberá a separação gradual dos componentes hoje preservados em `pages/test-quest.css`, como botões, campos, painéis, badges e modais.

A migração estrutural foi feita sem quebrar a interface atual. A divisão por componente deve acontecer em uma etapa própria, acompanhada por testes visuais.
